'use client';import{useEffect,useState}from'react';import{CalendarClock,Trash2,Save}from'lucide-react';
type Row={id:string;title:string;category:string;day:string;time:string;staff:string;notes:string;published:boolean};const cats=['Rujukan / Poli','VK / MTBS','Bidan Jaga','Skrining BPJS','Lainnya'];export default function ScheduleManager(){const[items,setItems]=useState<Row[]>([]);const[title,setTitle]=useState('');const[category,setCategory]=useState(cats[0]);const[day,setDay]=useState('');const[time,setTime]=useState('');const[staff,setStaff]=useState('');const[notes,setNotes]=useState('');const[saved,setSaved]=useState(false);async function load(){try{const r=await fetch('/api/schedule?admin=1',{cache:'no-store'});const d=await r.json();if(r.ok)setItems(d.items||[])}finally{}}useEffect(()=>{load()},[]);async function add(){if(!title||!day||!time){alert('Judul, hari/shift dan jam wajib diisi.');return}const r=await fetch('/api/schedule',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({title,category,day,time,staff,notes,published:true})});const d=await r.json();if(!r.ok){alert(d.error||'Gagal menyimpan.');return}setTitle('');setDay('');setTime('');setStaff('');setNotes('');setSaved(true);load();setTimeout(()=>setSaved(false),1800)}async function remove(id:string){if(!confirm('Hapus jadwal ini?'))return;const r=await fetch('/api/schedule',{method:'DELETE',headers:{'Content-Type':'application/json'},body:JSON.stringify({id})});if(r.ok)load()}return <div className="contentManager"><div className="editorCard"><div className="editorTitle"><div><div className="eyebrow">SCHEDULE MANAGEMENT</div><h2>Tambah jadwal operasional</h2><p>Atur rujukan, poli, VK/MTBS, bidan jaga, dan skrining BPJS.</p></div><CalendarClock size={30}/></div><div className="formGrid"><label><span>Nama jadwal *</span><input className="field" value={title} onChange={e=>setTitle(e.target.value)} placeholder="Bidan jaga malam"/></label><label><span>Kategori</span><select className="field" value={category} onChange={e=>setCategory(e.target.value)}>{cats.map(c=><option key={c}>{c}</option>)}</select></label><label><span>Hari / shift *</span><input className="field" value={day} onChange={e=>setDay(e.target.value)} placeholder="Senin / Pagi / Senin–Sabtu"/></label><label><span>Jam *</span><input className="field" value={time} onChange={e=>setTime(e.target.value)} placeholder="07.30–12.00 WIB"/></label><label><span>Petugas</span><input className="field" value={staff} onChange={e=>setStaff(e.target.value)} placeholder="Nama petugas / tim"/></label><label className="full"><span>Catatan</span><textarea className="field textArea" value={notes} onChange={e=>setNotes(e.target.value)} placeholder="Keterangan tambahan..."/></label></div><div className="editorActions"><button className="btn primary" onClick={add}><Save size={16}/> Simpan jadwal</button>{saved&&<span className="saved">Tersimpan</span>}</div></div><div className="contentList"><div className="listHead"><div><b>Jadwal tersimpan</b><span>{items.length} jadwal</span></div></div>{items.map(x=><div className="contentRow" key={x.id}><div className="contentThumb"><CalendarClock/></div><div className="contentRowBody"><div className="newsMeta"><span>{x.category}</span><span>{x.day} · {x.time}</span></div><h3>{x.title}</h3><p>{x.staff?`Petugas: ${x.staff}. `:''}{x.notes}</p></div><button className="iconBtn danger" onClick={()=>remove(x.id)}><Trash2 size={17}/></button></div>)}</div></div>}
