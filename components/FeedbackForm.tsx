'use client';
import {useState} from 'react';
import {MessageSquareHeart,Star,Send} from 'lucide-react';
export default function FeedbackForm(){const [rating,setRating]=useState(0);const [form,setForm]=useState({name:'',category:'Saran',service:'',message:'',contact:''});const [state,setState]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();setState('Mengirim...');try{const r=await fetch('/api/feedback',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...form,rating})});const j=await r.json();if(!r.ok)throw new Error(j.error||'Gagal');setState('Terima kasih! Masukan Anda sudah tercatat.');setForm({name:'',category:'Saran',service:'',message:'',contact:''});setRating(0)}catch(e:any){setState(e.message||'Gagal menyimpan. Silakan coba lagi.')}};return <section className="feedbackSection"><div className="feedbackIntro"><div className="eyebrow"><MessageSquareHeart size={15}/> SUARA PENGUNJUNG</div><h2>Keluhan, saran & penilaian pelayanan</h2><p>Bantu Puskesmas Somagede meningkatkan kualitas pelayanan. Anda boleh mengirim masukan tanpa nama.</p></div><form className="feedbackForm" onSubmit={submit}><div className="ratingRow"><span>Bagaimana pengalaman Anda?</span><div>{[1,2,3,4,5].map(n=><button type="button" aria-label={`${n} bintang`} className={n<=rating?'star active':'star'} onClick={()=>setRating(n)} key={n}><Star size={25} fill={n<=rating?'currentColor':'none'}/></button>)}</div></div><div className="feedbackFields"><input className="field" placeholder="Nama (opsional)" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><select className="field" value={form.category} onChange={e=>setForm({...form,category:e.target.value})}><option>Saran</option><option>Keluhan</option><option>Apresiasi</option></select><input className="field" placeholder="Layanan yang dinilai (opsional)" value={form.service} onChange={e=>setForm({...form,service:e.target.value})}/><input className="field" placeholder="Kontak (opsional)" value={form.contact} onChange={e=>setForm({...form,contact:e.target.value})}/></div><textarea className="field feedbackText" required minLength={5} maxLength={1500} placeholder="Tuliskan saran atau keluhan Anda..." value={form.message} onChange={e=>setForm({...form,message:e.target.value})}/><div className="feedbackBottom"><span className="hint">Mohon tidak menuliskan NIK, nomor rekam medis, atau data kesehatan pribadi.</span><button className="btn primary" disabled={!rating||!form.message.trim()}><Send size={16}/> Kirim masukan</button></div>{state&&<div className="badge">{state}</div>}</form></section>}
