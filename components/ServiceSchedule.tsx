'use client';
import { useEffect, useMemo, useState } from 'react';
import { CalendarClock, CheckCircle2, Clock3 } from 'lucide-react';
const hours:Record<number,[string,string,string]>={0:['Minggu','Tutup','Tutup'],1:['Senin','07.30','12.00'],2:['Selasa','07.30','12.00'],3:['Rabu','07.30','12.00'],4:['Kamis','07.30','12.00'],5:['Jumat','07.30','11.00'],6:['Sabtu','07.30','12.00']};
function parts(date:Date){const f=new Intl.DateTimeFormat('en-US',{timeZone:'Asia/Jakarta',weekday:'short',hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false}).formatToParts(date);const get=(t:string)=>f.find(x=>x.type===t)?.value||'';const map:any={Sun:0,Mon:1,Tue:2,Wed:3,Thu:4,Fri:5,Sat:6};return {day:map[get('weekday')],hour:Number(get('hour')),minute:Number(get('minute')),second:Number(get('second'))};}
const toMin=(s:string)=>{const [h,m]=s.split(':').map(Number);return h*60+m};
export default function ServiceSchedule(){const [now,setNow]=useState(new Date());useEffect(()=>{const id=setInterval(()=>setNow(new Date()),1000);return()=>clearInterval(id)},[]);const x=parts(now);const s=hours[x.day];const current=x.hour*60+x.minute;const open=s[1]!=='Tutup'&&current>=toMin(s[1])&&current<toMin(s[2]);const date=useMemo(()=>new Intl.DateTimeFormat('id-ID',{timeZone:'Asia/Jakarta',weekday:'long',day:'2-digit',month:'long',year:'numeric'}).format(now),[now]);const time=new Intl.DateTimeFormat('id-ID',{timeZone:'Asia/Jakarta',hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false}).format(now);return <div className="scheduleWidget"><div className="scheduleNow"><div><div className="eyebrow"><CalendarClock size={15}/> Jadwal real-time WIB</div><h3>{date}</h3><p>Waktu sekarang <b>{time} WIB</b></p></div><div className={`openBadge ${open?'isOpen':''}`}><span>{open?<CheckCircle2 size={16}/>:<Clock3 size={16}/>}</span>{open?'Sedang buka':'Di luar jam pendaftaran'}</div></div><div className="scheduleToday"><b>{s[0]}</b><span>Pendaftaran {s[1]} – {s[2]} WIB</span></div><div className="scheduleList">{[1,2,3,4,5,6].map(d=><div key={d}><span>{hours[d][0]}</span><b>{hours[d][1]} – {hours[d][2]}</b></div>)}</div></div>}
