'use client';
import PageTransition from '@/components/PageTransition'; import { Bot, Send, Sparkles, Clock3, MapPin, Stethoscope } from 'lucide-react'; import { useState } from 'react';
type Msg={role:'user'|'assistant';content:string};
const quick=['Jam pendaftaran hari ini?','Apa saja layanan Puskesmas?','Di mana lokasi Puskesmas Somagede?','Apa yang harus dibawa saat berobat?'];
export default function Chatbot(){const [input,setInput]=useState('');const [loading,setLoading]=useState(false);const [msgs,setMsgs]=useState<Msg[]>([{role:'assistant',content:'Halo 👋 Saya Asisten Puskesmas Somagede. Saya siap membantu mencari informasi layanan, jadwal, lokasi, dan informasi publik.'}]);
 async function send(text=input){const q=text.trim();if(!q||loading)return;const next=[...msgs,{role:'user',content:q} as Msg];setMsgs(next);setInput('');setLoading(true);try{const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({messages:next})});const d=await r.json();setMsgs(m=>[...m,{role:'assistant',content:d.reply||'Maaf, belum ada jawaban.'}]);}catch{setMsgs(m=>[...m,{role:'assistant',content:'Maaf, koneksi ke Asisten sedang bermasalah.'}]);}finally{setLoading(false)}}
 return <PageTransition><main><section className="pageHero chatbotHero"><div className="container"><div className="eyebrow"><Sparkles size={15}/> ASISTEN DIGITAL AI</div><h1>Teman informasi <span>Puskesmas Somagede.</span></h1><p>Tanyakan jadwal, layanan, lokasi, atau informasi publik. AI berjalan melalui server sehingga API key tidak diletakkan di browser.</p></div></section><section className="section"><div className="container chatbotWrap aiChat"><div className="chatHeader"><div className="botIcon"><Bot/></div><div><b>Asisten Somagede</b><span>{loading?'Sedang mengetik…':'Siap membantu'}</span></div><div className="aiPulse">AI</div></div><div className="quickPrompts">{quick.map((q,i)=><button key={q} onClick={()=>send(q)} disabled={loading}>{i===0?<Clock3 size={15}/>:i===1?<Stethoscope size={15}/>:<MapPin size={15}/>} {q}</button>)}</div><div className="chatMessages">{msgs.map((m,i)=><div key={i} className={`chatMsg ${m.role==='user'?'user':'bot'}`}>{m.content}</div>)}{loading&&<div className="chatMsg bot typing"><i/><i/><i/></div>}</div><div className="chatComposer"><input value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Tulis pertanyaan untuk Asisten…"/><button onClick={()=>send()} aria-label="Kirim" disabled={loading}><Send size={18}/></button></div></div></section></main></PageTransition>}
