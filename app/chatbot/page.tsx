'use client';
import PageTransition from '@/components/PageTransition'; import { Bot, Send, Sparkles } from 'lucide-react'; import { useState } from 'react';
export default function Chatbot(){const [input,setInput]=useState('');const [msgs,setMsgs]=useState([{from:'bot',text:'Halo 👋 Saya calon Asisten Puskesmas Somagede. Fitur chatbot ini sudah disiapkan dan bisa dihubungkan ke AI/API nanti.'}]);function send(){if(!input.trim())return;setMsgs(m=>[...m,{from:'user',text:input.trim()},{from:'bot',text:'Pesan diterima. Nantinya bagian ini bisa dihubungkan ke knowledge base dan AI assistant Puskesmas.'}]);setInput('')}return <PageTransition><main><section className="pageHero"><div className="container"><div className="eyebrow"><Sparkles size={15}/> ASISTEN DIGITAL</div><h1>Asisten Puskesmas</h1><p>Ruang chatbot yang siap dikembangkan untuk menjawab pertanyaan layanan dan informasi kesehatan.</p></div></section><section className="section"><div className="container chatbotWrap"><div className="chatHeader"><div className="botIcon"><Bot/></div><div><b>Asisten Somagede</b><span>Siap membantu</span></div></div><div className="chatMessages">{msgs.map((m,i)=><div key={i} className={`chatMsg ${m.from}`}>{m.text}</div>)}</div><div className="chatComposer"><input value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Tulis pertanyaan..."/><button onClick={send} aria-label="Kirim"><Send size={18}/></button></div></div></section></main></PageTransition>}
