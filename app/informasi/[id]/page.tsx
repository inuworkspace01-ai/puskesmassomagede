'use client';
import { useEffect, useState } from 'react'; import Link from 'next/link'; import { ArrowLeft, CalendarDays } from 'lucide-react';
export default function Detail({params}:{params:Promise<{id:string}>}){const [item,setItem]=useState<any>(null);useEffect(()=>{params.then(({id})=>{try{const x=JSON.parse(localStorage.getItem('somagede_contents')||'[]');setItem(x.find((n:any)=>n.id===decodeURIComponent(id)))}catch{}})},[params]);return <main className="detailPage"><div className="container"><Link href="/informasi" className="backLink"><ArrowLeft size={16}/> Kembali ke informasi</Link>{item?<article className="detailCard">{item.image&&<img src={item.image} alt=""/>}<div className="detailBody"><div className="newsMeta"><span>{item.category}</span><span><CalendarDays size={13}/>{item.date}</span></div><h1>{item.title}</h1><p>{item.description}</p></div></article>:<div className="emptyState">Konten belum tersedia atau belum dipublikasikan.</div>}</div></main>}
